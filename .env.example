# Скопируйте этот файл в .env и поменяйте пароль. Файл .env в git не попадает.
ADMIN_PASSWORD=поменяйте-меня
PORT=3000
# Публичный адрес для QR-кода, например https://unicorn-arena.emorozoff.ru (пусто — берётся из запроса)
PUBLIC_URL=
